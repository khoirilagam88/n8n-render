services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      # Akses web n8n
      - N8N_HOST=0.0.0.0
      - N8N_PORT=5678
      - N8N_PROTOCOL=http

      # Zona waktu Indonesia
      - GENERIC_TIMEZONE=Asia/Jakarta
      - TZ=Asia/Jakarta

      # Aktifkan login keamanan
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=khoiril201188

      # Mode produksi
      - NODE_ENV=production

    volumes:
      # Folder penyimpanan konfigurasi n8n (workflow, credential, dsb)
      - ./data:/home/node/.n8n
      # Folder tempat file Excel atau data kamu
      - ./files:/data
